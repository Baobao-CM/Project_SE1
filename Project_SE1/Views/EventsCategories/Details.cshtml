@model project_dnc_se1.Models.EventsCategory

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml"; // đường dẫn tới layout admin
    ViewData["Title"] = "Chi tiết danh mục Sự kiện";

}

<h1>Chi tiết danh mục sự kiện</h1>

<div>
    <h4>EventsCategory</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Quay lại danh sách</a>
</div>
